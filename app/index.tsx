import { Auth } from '../components/Auth';
import { Box } from '@/components/ui/box';

export default function Index() {
  return (
    <Box className="flex-1 justify-center items-center bg-[#596558]">
      <Auth />
    </Box>
  );
}